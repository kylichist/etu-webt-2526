services:
  web:
    build: .
    container_name: lb2
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./public/covers:/app/public/covers
      - ./public:/app/public
    environment:
      - PORT=3000
