# Лабораторная работа 5 - Биржа Брокеров

Веб-приложение для имитации биржи брокера с использованием NestJS, React, Redux, WebSocket и Chart.js.

## Технологии

### Backend
- **NestJS** - современный фреймворк для Node.js
- **TypeScript** - типизированный JavaScript
- **WebSocket (Socket.io)** - для real-time обновлений цен
- **JSON файлы** - для хранения данных

### Frontend
- **React 18** - библиотека для UI
- **TypeScript** - типизация
- **Redux Toolkit** - управление состоянием
- **React Router DOM** - маршрутизация
- **Material UI** - компоненты интерфейса
- **Chart.js** - графики и диаграммы
- **Vite** - быстрый сборщик

## Структура проекта

```
lb5/
├── server/              # NestJS backend
│   ├── src/
│   │   ├── brokers/    # Модуль брокеров (CRUD)
│   │   ├── stocks/     # Модуль акций
│   │   ├── trading/    # Модуль торгов + WebSocket
│   │   └── auth/       # Базовая аутентификация (бонус)
│   └── package.json
├── client/              # React frontend
│   ├── src/
│   │   ├── components/ # React компоненты
│   │   ├── store/      # Redux slices
│   │   └── services/   # API и WebSocket
│   └── package.json
├── data/                # JSON данные
│   ├── brokers.json
│   ├── stocks.json
│   └── settings.json
├── Dockerfile
└── docker-compose.yml
```

## Установка и запуск

### Локальная разработка

1. Установить зависимости:
```bash
cd lb5
npm run install:all
```

2. Запустить в режиме разработки:
```bash
npm run dev
```

Backend: http://localhost:3001
Frontend: http://localhost:3000

### Запуск через Docker

```bash
cd lb5
docker-compose up --build
```

## Функционал

### Страница Брокеров (`/brokers`)
- ✅ Просмотр списка брокеров
- ✅ Добавление нового брокера (имя, начальный капитал)
- ✅ Редактирование брокера
- ✅ Удаление брокера
- ✅ Валидация данных

### Страница Акций (`/stocks`)
- ✅ Список всех акций (8 компаний: AAPL, SBUX, MSFT, CSCO, QCOM, AMZN, TSLA, AMD)
- ✅ Выбор акций для участия в торгах
- ✅ Таблица с историческими данными
- ✅ Графики изменения цен (Chart.js)
- ✅ Данные за 2020-2021 годы

### Страница Торгов (`/trading`)
- ✅ Настройка даты начала торгов
- ✅ Настройка скорости изменения дат (в секундах)
- ✅ Кнопка "Начать торги" / "Остановить"
- ✅ Отображение текущей даты торгов
- ✅ Отображение текущих цен акций в реальном времени
- ✅ WebSocket для real-time обновлений

## API Endpoints

### Brokers
- `GET /api/brokers` - получить всех брокеров
- `POST /api/brokers` - создать брокера
- `PUT /api/brokers/:id` - обновить брокера
- `DELETE /api/brokers/:id` - удалить брокера

### Stocks
- `GET /api/stocks` - получить все акции
- `PUT /api/stocks/:id/selection` - изменить выбор акции

### Trading
- `GET /api/trading/settings` - получить настройки
- `PUT /api/trading/settings` - обновить настройки
- `POST /api/trading/start` - начать торги
- `POST /api/trading/stop` - остановить торги
- `GET /api/trading/prices` - получить текущие цены

### WebSocket
- Event: `priceUpdate` - обновление цен в реальном времени

## Особенности

- 📱 **Responsive дизайн** - корректная работа на мобильных устройствах
- 🎨 **Material UI** - современный дизайн интерфейса
- ⚡ **Real-time обновления** - через WebSocket
- 📊 **Графики** - визуализация данных с Chart.js
- ✅ **Валидация** - проверка всех вводимых данных
- 🔄 **Redux** - централизованное управление состоянием
- 🐳 **Docker** - простое развертывание

## Примечания

- Учебный проект - CSP и production-ограничения отключены
- Данные хранятся в JSON файлах
- Базовая аутентификация (Auth модуль) - подготовлена структура для расширения
