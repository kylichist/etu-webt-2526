version: '3.8'

services:
  stock-exchange:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3001
    restart: unless-stopped
